if(CONFIG_SHIELD_DONGLE_SCREEN)
  zephyr_library()
  zephyr_library_sources(${ZEPHYR_BASE}/misc/empty_file.c)
  zephyr_library_include_directories(${ZEPHYR_LVGL_MODULE_DIR})
  zephyr_library_include_directories(${ZEPHYR_BASE}/lib/gui/lvgl/)
  zephyr_library_include_directories(${ZEPHYR_BASE}/drivers)
  zephyr_library_include_directories(${CMAKE_SOURCE_DIR}/include)
  zephyr_library_include_directories(${ZEPHYR_CURRENT_MODULE_DIR}/include)
  zephyr_library_include_directories(${ZEPHYR_CURRENT_CMAKE_DIR}/include)
  zephyr_library_include_directories(include)
  zephyr_library_include_directories(src)

  if(CONFIG_DONGLE_SCREEN_PROFILE_EVA)
    set(PROFILE_DIR src/profiles/eva)
  else()
    set(PROFILE_DIR src/profiles/default)
  endif()

  zephyr_library_include_directories(${PROFILE_DIR}/widgets)
  zephyr_library_sources(src/brightness.c)
  zephyr_library_sources(src/screen_rotate_init.c)
  zephyr_library_sources(${PROFILE_DIR}/custom_status_screen.c)
  zephyr_library_sources(${PROFILE_DIR}/widgets/output_status.c)
  zephyr_library_sources(${PROFILE_DIR}/widgets/battery_status.c)
  zephyr_library_sources(${PROFILE_DIR}/widgets/layer_status.c)
  zephyr_library_sources(${PROFILE_DIR}/widgets/wpm_status.c)
  zephyr_library_sources(${PROFILE_DIR}/widgets/mod_status.c)
  file(GLOB font_sources src/fonts/*.c)
  zephyr_library_sources(${font_sources})
  if(CONFIG_DONGLE_SCREEN_BACKGROUND_IMAGE)
    file(GLOB image_sources ${PROFILE_DIR}/images/*.c)
    zephyr_library_sources(${image_sources})
  endif()
endif()
